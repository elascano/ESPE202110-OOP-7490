package ec.edu.espe.amperage;

/**
 *
 * @author Joel Zeas
 */
public class CircuitAmperge {
    public float less(float dividend, float divider){
        float quotient;
        //a complex computing algorithm is here
        quotient= dividend / divider;
        return quotient;
    }
    
     public float tenth(float dividend, float divider){
        float quotient;
        //a complex computing algorithm is here
        quotient= dividend / divider;
        return quotient;
    }
     
     public float zero(float dividend, float divider){
        float quotient;
        //a complex computing algorithm is here
        quotient= dividend / divider;
        return quotient;
    }
     
     public float error(float dividend, float divider){
        float quotient;
        //a complex computing algorithm is here
        quotient= dividend / divider;
        return quotient;
    }
     
     public float point(float dividend, float divider){
        float quotient;
        //a complex computing algorithm is here
        quotient= dividend / divider;
        return quotient;
    }
     
     public float negative(float dividend, float divider){
        float quotient;
        //a complex computing algorithm is here
        quotient= dividend / divider;
        return quotient;
    }
     
     public float approximate(float dividend, float divider){
        float quotient;
        //a complex computing algorithm is here
        quotient= dividend / divider;
        return quotient;
    }
     
     public float values(float dividend, float divider){
        float quotient;
        //a complex computing algorithm is here
        quotient= dividend / divider;
        return quotient;
    }
     
     public float figure(float dividend, float divider){
        float quotient;
        //a complex computing algorithm is here
        quotient= dividend / divider;
        return quotient;
    }
     
     public float near(float dividend, float divider){
        float quotient;
        //a complex computing algorithm is here
        quotient= dividend / divider;
        return quotient;
    }

}
